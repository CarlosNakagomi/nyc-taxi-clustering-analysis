# Python 3.10+ (3.14 supported; pure wheels only — no C++ build tools required)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18
folium>=0.15
# Optional: static PNG export for Plotly maps (pip may install a binary wheel)
kaleido>=0.2.1
